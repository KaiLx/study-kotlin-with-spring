machine:
  timezone:
    Asia/Tokyo
  java:
    version: oraclejdk8
  environment:
    _JAVA_OPTIONS: "-Xms1024m -Xmx3072m"
dependencies:
  override:
    - mvn clean
test:
  override:
    - mvn clean
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - echo test
